#include "main.h"

/**
 * _isupper - this is the original function
 * @c: an argument we pass
 * Return: the value returned by the program is 0 or 1
 */

int _isupper(int c)
{
	return (c >= 'A' && c <= 'Z');
}
