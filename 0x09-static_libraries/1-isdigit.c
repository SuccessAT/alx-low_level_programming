#include "main.h"

/**
 * _isdigit - this is a c function
 * @c: an argument we pass
 * Return: the value returned by the program is 0 or 1
 */

int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}
