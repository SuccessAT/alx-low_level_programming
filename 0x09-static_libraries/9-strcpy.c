#include "main.h"
#include <string.h>
/**
 * _strcpy - a c function to concatenate two strings
 * @dest: first string
 * @src: second string
 *
 * Return: a string
 */
char *_strcpy(char *dest, char *src)
{
	return (dest);
}
