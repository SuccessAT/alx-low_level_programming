#include "main.h"
#include <string.h>

/**
 * wildcmp - a c function
 * @s1: a parameter
 * @s2: a second parameter
 *
 * Return: an integer
 */

int wildcmp(char *s1, char *s2)
{
	if (s1 == '\0' || s2 == '\0')
		return (-2);

	if (strcmp(s1,s2) == 0)
		return (0);

	if ((s1[0]) == (s
	{
		return (1);
	}

	return (x * _pow_recursion(x, y - 1));
}
