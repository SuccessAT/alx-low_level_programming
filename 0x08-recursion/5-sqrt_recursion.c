#include "main.h"

/**
 * _sqrt_recursion - a recursive function to find the square root
 * @n: integer parameter
 *
 * Return: an integer
 */

int _sqrt_recursion(int n)
{
	unsigned int i = n/2;
	unsigned int j

	if (**2 < n)
	{
		i = -1;
		return (i);
	}

	if (n == 0)
	{
		return (1);
	}
	else
	{
		
		return (_sqrt_recursion(n));
	}
}
