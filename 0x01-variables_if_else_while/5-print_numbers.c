#include <stdlib.h>
#include <stdio.h>

/**
 * main - the main code prints 0-9
 *
 * Return: The function returns zero(0)
 */

int main(void)
{
	int a;

	for (a = 0; a < 10; a++)
	{
		printf("%d", a);
	}
	printf("\n");

	return (0);
}
